# ✏️ Быстрый старт: Редактирование и деплой

## Рабочий процесс (без локального запуска)

Поскольку вы выбрали вариант **только Railway**, локально запускать ничего не нужно.

### 1️⃣ Редактирование кода

Открывайте файлы в VSCode и редактируйте как обычно:

```bash
cd "/Users/legend/Desktop/PRODIGY/apps venture/content-pipeline"
code .
```

**Что можно делать:**
- ✅ Редактировать TypeScript файлы
- ✅ Изменять React компоненты
- ✅ Обновлять Prisma схему
- ✅ Добавлять новые сервисы и роуты
- ✅ Писать логику Playwright для публикации

**TypeScript ошибки в редакторе:**
- Это нормально! Локально зависимости не установлены из-за проблемы с esbuild
- Код **всё равно скомпилируется и запустится на Railway** (там Linux с правильными либами)
- Можете игнорировать красные подчёркивания в VSCode

### 2️⃣ Проверка изменений

```bash
# Посмотреть что изменилось
git status

# Посмотреть diff
git diff
```

### 3️⃣ Сохранение изменений

```bash
# Добавить все файлы
git add .

# Или выборочно
git add packages/api/src/routes/articles.ts

# Коммит с описанием
git commit -m "Добавил поддержку обложек для статей"
```

### 4️⃣ Деплой на Railway

```bash
# Отправить код на GitHub
git push origin main
```

**Всё!** Railway автоматически:
1. Заметит новый коммит (1-5 секунд)
2. Начнёт сборку (~2-3 минуты)
3. Задеплоит новую версию
4. Покажет в логах результат

### 5️⃣ Проверка деплоя

Откройте [Railway Dashboard](https://railway.app/dashboard):

- **Deployments** → смотрите статус (Building / Deploying / Success / Failed)
- **Logs** → смотрите вывод сборки и runtime логи
- Если ошибка → скопируйте лог, исправьте код, снова `git push`

---

## 📂 Структура проекта (что и где менять)

### Backend (API):

```
packages/api/src/
├── routes/              # Эндпоинты API
│   ├── articles.ts      # CRUD статей
│   ├── pipeline.ts      # Запуск этапов конвейера
│   ├── publishing.ts    # Публикация в соцсети
│   └── config.ts        # Настройки и шаблоны
├── services/
│   ├── ai/
│   │   ├── research.ts  # Сбор информации о знаменитости
│   │   └── generator.ts # Генерация статьи
│   ├── media/
│   │   └── cover.ts     # Создание обложки
│   └── publishers/
│       ├── index.ts     # Главный диспетчер публикаций
│       ├── playwright.ts # Playwright автоматизация
│       ├── telegram.ts  # Telegram публикация
│       └── vk.ts        # VK публикация
└── index.ts             # Запуск Express сервера
```

### Frontend (React):

```
packages/web/src/
├── pages/
│   ├── Dashboard.tsx    # Главная страница со списком статей
│   ├── NewArticle.tsx   # Форма создания статьи
│   └── ArticleDetail.tsx # Детали статьи и контроль этапов
├── components/
│   ├── PipelineProgress.tsx # Прогресс-бар этапов
│   ├── ResearchView.tsx     # Показ результатов research
│   ├── ContentView.tsx      # Показ сгенерированной статьи
│   └── PublishingView.tsx   # Статус публикаций
└── lib/
    └── api.ts           # Axios клиент для API запросов
```

### Общие типы:

```
packages/shared/src/
└── types.ts             # TypeScript типы для всего проекта
```

---

## 🔄 Типичные изменения

### Добавить новую платформу для публикации

1. **Добавить в enum** (`packages/shared/src/types.ts`):
```typescript
export enum Platform {
  TELEGRAM = 'telegram',
  VK = 'vk',
  INSTAGRAM = 'instagram',
  TIKTOK = 'tiktok', // ← новый
  // ...
}
```

2. **Создать publisher** (`packages/api/src/services/publishers/tiktok.ts`):
```typescript
export async function publishToTikTok(article: Article) {
  // Логика публикации
}
```

3. **Добавить в маршрут** (`packages/api/src/services/publishers/playwright.ts`):
```typescript
case Platform.TIKTOK:
  return await publishToTikTok(browser, article);
```

4. Коммит и пуш → готово!

### Изменить промпт для AI

Файл: `packages/api/src/services/ai/generator.ts`

```typescript
const prompt = `
Твой новый улучшенный промпт...
`;
```

### Добавить новое API поле в статью

1. Обновить Prisma схему (`packages/api/prisma/schema.prisma`):
```prisma
model Article {
  // ...
  newField String? // ← добавить
}
```

2. Обновить типы (`packages/shared/src/types.ts`):
```typescript
export interface Article {
  // ...
  newField?: string; // ← добавить
}
```

3. Коммит, пуш → Railway запустит миграцию автоматически

---

## 🐛 Дебаг на Railway

### Смотреть логи в реальном времени:

**Через Dashboard:**
- Railway → Ваш проект → API service → Logs (live)

**Через CLI:**
```bash
npm i -g @railway/cli
railway login
railway link
railway logs
```

### Добавить логирование в код:

```typescript
console.log('🔍 Researching celebrity:', celebrityName);
console.error('❌ Error generating article:', error);
console.info('✅ Published to', platform, ':', result.url);
```

Эти логи появятся в Railway Dashboard.

### Выполнить команду в контейнере:

```bash
railway run npm run db:studio --workspace=packages/api
```

Откроет Prisma Studio для просмотра БД.

---

## 📝 Полезные команды Git

```bash
# Отменить последние изменения (до коммита)
git checkout -- packages/api/src/routes/articles.ts

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1

# Посмотреть историю коммитов
git log --oneline

# Создать ветку для эксперимента
git checkout -b feature/new-publisher
# ... делаете изменения ...
git push origin feature/new-publisher
# Потом мержите в main через GitHub PR

# Скачать последние изменения (если редактировали на другом компе)
git pull origin main
```

---

## ⚡ Быстрые фиксы

### Railway показывает ошибку компиляции:

1. Скопируйте ошибку из логов
2. Загуглите или спросите ChatGPT
3. Исправьте код
4. `git add . && git commit -m "fix: compile error" && git push`

### База данных не синхронизирована:

```bash
railway link
railway run npm run db:migrate --workspace=packages/api
```

### Нужно срочно откатить деплой:

Railway Dashboard → Deployments → выберите предыдущий успешный деплой → **Redeploy**

---

## 🎯 Workflow пример

**Задача:** Добавить поддержку Яндекс.Дзен

```bash
# 1. Создать ветку
git checkout -b feature/dzen-publisher

# 2. Добавить Platform.DZEN в types.ts
# 3. Создать publishers/dzen.ts с логикой
# 4. Обновить playwright.ts добавив case для Dzen

# 5. Коммит
git add .
git commit -m "feat: добавлена публикация в Яндекс.Дзен"

# 6. Пуш
git push origin feature/dzen-publisher

# 7. Railway автоматически создаст preview deployment для этой ветки
# 8. Тестируете на preview URL
# 9. Если всё ОК → мерж в main через GitHub
# 10. Railway автоматически задеплоит в прод
```

---

## 💡 Советы

✅ **Делайте коммиты часто** — каждое логическое изменение отдельным коммитом  
✅ **Пишите понятные commit messages** — потом легче искать что сломалось  
✅ **Смотрите логи Railway после каждого деплоя** — ловите ошибки сразу  
✅ **Используйте feature branches** для больших изменений  
✅ **Бэкапьте данные** — периодически делайте дамп БД  

❌ **Не коммитьте .env файлы** — только .env.example  
❌ **Не храните API ключи в коде** — только через переменные окружения Railway  
❌ **Не пушьте node_modules** — уже в .gitignore  

---

## 🆘 Если что-то сломалось

1. Посмотрите логи на Railway
2. Скопируйте ошибку
3. Откатите на предыдущий деплой (если критично)
4. Исправьте локально
5. Пушьте фикс

Или напишите мне — помогу разобраться! 😉
