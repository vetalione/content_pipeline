# 🚂 Развёртывание на Railway

## 📋 Предварительные требования

1. Аккаунт на [Railway.app](https://railway.app)
2. GitHub аккаунт
3. Репозиторий с кодом проекта на GitHub

---

## 🎯 Пошаговая инструкция

### Шаг 1: Загрузить код в GitHub

```bash
cd "/Users/legend/Desktop/PRODIGY/apps venture/content-pipeline"

# Инициализировать git (если ещё не сделано)
git init
git add .
git commit -m "Initial commit: Content Pipeline"

# Создайте новый репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/content-pipeline.git
git branch -M main
git push -u origin main
```

### Шаг 2: Создать проект на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Авторизуйте Railway доступ к вашим репозиториям
5. Выберите репозиторий `content-pipeline`

### Шаг 3: Добавить сервисы

Railway автоматически создаст один сервис для вашего API. Теперь добавьте базу данных и Redis:

#### 3.1 Добавить PostgreSQL

1. В проекте нажмите **"+ New"**
2. Выберите **"Database" → "Add PostgreSQL"**
3. Railway автоматически создаст базу и переменную `DATABASE_URL`

#### 3.2 Добавить Redis

1. Нажмите **"+ New"** снова
2. Выберите **"Database" → "Add Redis"**
3. Railway создаст переменную `REDIS_URL`

### Шаг 4: Настроить переменные окружения

В настройках вашего API сервиса добавьте следующие переменные:

#### Обязательные:

```bash
# Эти уже будут созданы автоматически Railway:
DATABASE_URL=postgresql://...  # от PostgreSQL сервиса
REDIS_URL=redis://...          # от Redis сервиса

# Добавьте вручную:
NODE_ENV=production
PORT=3001

# OpenAI (обязательно!)
OPENAI_API_KEY=sk-...

# JWT и безопасность
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
```

#### Опциональные (для расширенного функционала):

```bash
# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini
GOOGLE_AI_API_KEY=...

# ElevenLabs TTS (для озвучки)
ELEVENLABS_API_KEY=...

# Image Search APIs
GOOGLE_SEARCH_API_KEY=...
GOOGLE_SEARCH_ENGINE_ID=...
BING_SEARCH_API_KEY=...

# Social Media (для публикации)
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHANNEL_ID=...
VK_ACCESS_TOKEN=...
VK_GROUP_ID=...

# Playwright (для автоматизации браузера)
PLAYWRIGHT_HEADLESS=true
```

### Шаг 5: Настроить домены (опционально)

1. В настройках API сервиса найдите раздел **"Settings" → "Networking"**
2. Railway автоматически даст вам домен типа: `https://your-app.up.railway.app`
3. Можно добавить свой кастомный домен

### Шаг 6: Подключить Frontend к API

После деплоя API получите его URL (например, `https://content-pipeline-api.up.railway.app`)

Обновите файл `packages/web/src/lib/api.ts`:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://content-pipeline-api.up.railway.app';
```

Или создайте `.env` для web пакета:

```bash
VITE_API_URL=https://content-pipeline-api.up.railway.app
```

### Шаг 7: Развернуть Frontend

**Вариант A: Vercel (рекомендуется для React)**

1. Зайдите на [vercel.com](https://vercel.com)
2. Import репозитория
3. Укажите:
   - **Root Directory**: `packages/web`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. Добавьте переменную окружения `VITE_API_URL`
5. Deploy!

**Вариант B: Netlify**

Аналогично Vercel, но в Netlify.

**Вариант C: Railway (второй сервис)**

1. В том же Railway проекте нажмите **"+ New Service"**
2. Выберите тот же GitHub репо
3. В настройках укажите:
   - **Start Command**: `npm run preview --workspace=packages/web`
   - **Build Command**: `npm run build --workspace=packages/web`

---

## 🔧 Миграция базы данных

После первого деплоя нужно запустить миграции Prisma:

### Через Railway CLI:

```bash
# Установить Railway CLI
npm i -g @railway/cli

# Войти
railway login

# Выбрать проект
railway link

# Запустить миграцию
railway run npm run db:migrate --workspace=packages/api
```

### Через Railway Dashboard:

1. Зайдите в настройки API сервиса
2. Откройте вкладку **"Variables"**
3. Временно добавьте переменную: `RAILWAY_RUN_BUILD_COMMAND=npm run db:migrate --workspace=packages/api`
4. Сделайте redeploy
5. Удалите эту переменную после успешного деплоя

---

## 🚀 Автоматический деплой

Railway автоматически деплоит при каждом `git push` в ветку `main`.

**Workflow:**

1. Пишете код локально
2. Коммитите изменения
3. `git push origin main`
4. Railway автоматически:
   - Собирает проект
   - Запускает тесты (если есть)
   - Деплоит новую версию
   - Откатывается на предыдущую версию при ошибке

---

## 📊 Мониторинг и логи

### Просмотр логов:

1. В Railway Dashboard откройте ваш сервис
2. Вкладка **"Deployments"** → выберите деплой → **"View Logs"**

### Метрики:

Railway показывает:
- CPU usage
- Memory usage
- Network traffic
- Deployment history

---

## 🔐 Авторизация Playwright на продакшене

Для автоматизации публикаций через браузер нужны сохранённые сессии.

### Проблема:
Playwright запускается headless на Railway — нельзя видеть браузер и логиниться вручную.

### Решение:

1. **Локально авторизуйтесь один раз:**

```bash
# Установите Playwright локально (только для авторизации)
npm install -D playwright
npx playwright install chromium

# Запустите скрипт авторизации
node packages/api/scripts/setup-auth.js vk
```

2. **Скопируйте файлы сессий:**

После авторизации будут созданы файлы:
```
packages/api/src/services/publishers/sessions/
├── vk-state.json
├── instagram-state.json
├── youtube-state.json
└── ...
```

3. **Загрузите на Railway:**

Можно закоммитить эти файлы в **приватный** репо (НЕ публичный!) или:

- Использовать Railway Volumes для персистентного хранения
- Загрузить через API при первом запуске
- Использовать переменные окружения (если файлы маленькие, можно base64)

**Пример через переменную окружения:**

```bash
# Локально
cat packages/api/src/services/publishers/sessions/vk-state.json | base64

# Добавить в Railway:
VK_SESSION_STATE=eyJjb29raWVzIjpbXSwi...
```

И в коде декодировать при старте.

---

## 💰 Стоимость

Railway:
- **Hobby Plan**: $5/месяц + pay-as-you-go
- Включает $5 кредитов
- ~500 часов работы базовых сервисов

**Ваш проект (примерная оценка):**
- API: ~$3-5/месяц
- PostgreSQL: ~$2-3/месяц  
- Redis: ~$1-2/месяц
- **Итого**: ~$6-10/месяц

Vercel/Netlify для Frontend — бесплатно (hobby tier).

---

## 🐛 Troubleshooting

### "Build failed: esbuild error"
✅ **Решено**: `nixpacks.toml` настроен на Node.js 20 с правильными системными библиотеками

### "Prisma Client not generated"
```bash
railway run npm run db:generate --workspace=packages/api
```

### "Cannot connect to database"
Проверьте что переменная `DATABASE_URL` автоматически передана от PostgreSQL сервиса к API сервису:
1. Railway Dashboard → PostgreSQL service → Variables → скопируйте `DATABASE_URL`
2. API service → Variables → добавьте вручную если её нет

### "Redis connection timeout"
Аналогично с `REDIS_URL`

### "Playwright browser not found"
✅ **Решено**: `nixpacks.toml` устанавливает Chromium автоматически

### Логи не показывают ошибку
Добавьте детальное логирование:
```typescript
console.error('Error details:', error);
```

---

## 📝 Checklist перед деплоем

- [ ] Код загружен на GitHub
- [ ] `.env.example` обновлён (без реальных секретов!)
- [ ] `.gitignore` содержит `/sessions/` и `.env`
- [ ] `railway.json` и `nixpacks.toml` в корне репо
- [ ] PostgreSQL сервис добавлен
- [ ] Redis сервис добавлен
- [ ] `DATABASE_URL` настроена
- [ ] `REDIS_URL` настроена
- [ ] `OPENAI_API_KEY` добавлен
- [ ] Миграции запущены
- [ ] Frontend задеплоен на Vercel/Netlify
- [ ] `VITE_API_URL` указывает на Railway API

---

## 🎉 Готово!

После деплоя ваш контент-конвейер будет доступен 24/7.

**Обновления через git:**
```bash
git add .
git commit -m "Add new feature"
git push origin main
# Railway автоматически задеплоит через 2-3 минуты
```

**Полезные ссылки:**
- Railway Dashboard: https://railway.app/dashboard
- Railway Docs: https://docs.railway.app
- Prisma Deploy Guide: https://www.prisma.io/docs/guides/deployment
